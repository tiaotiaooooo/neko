# 访问转换器(access transformer)。
# 把原版类的私有方法临时放宽为 public,便于模组调用。
# 这里把 Ocelot 的 setTrusting(boolean) 从 private 放开为 public,
# 这样"香香软软"buff 效果里就能直接让豹猫信任玩家(不再逃跑)。

public net.minecraft.world.entity.animal.Ocelot setTrusting(Z)V
